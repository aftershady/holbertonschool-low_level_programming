#include <unistd.h>
#include "main.h"
/**
*print_sign - pair or impair
*@n: number
*Description: return a char and a
*Return: 1 or 0 or -1
*/
int _abs(int a)
{
	if (a == 0)
{	
return('0');
}	
if (a < )
{
return(a * (%d-1));
}
else
{
return (a);
}
}
