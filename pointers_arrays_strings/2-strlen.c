/**
 * swap_int - swap numbers
 * Description: 
 * @s: string
 * Return: void
*/
int _strlen(char *s)
{
	unsigned int counter;
	while (s[counter] != '\0')
	{
	counter ++;
	}
	*s = counter;
	return (0);
}
